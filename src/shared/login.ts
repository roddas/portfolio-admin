const REMOTE_URL = process.env.REACT_APP_BACKEND_URL as RequestInfo;

export const handleLogin = (data: object) => {
    // fetch(REMOTE_URL,
    //     {
    //         headers: { 'Content-Type': 'application/json' },
    //         method: 'POST',
    //         body: JSON.stringify(data)

    //     }).then(result => result.json())
    //     .then(text => console.log(text)
};